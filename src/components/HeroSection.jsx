const HeroSection = () => (
  <section className="border-4 border-blue-500 w-full h-screen bg-gray-200">
    <h1 className="text-4xl font-bold">Welcome to the Hero Section</h1>
  </section>
);

export default HeroSection;